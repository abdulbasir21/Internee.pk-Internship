# Base URL of the backend REST API
VITE_API_URL=http://localhost:5000/api

# Base URL of the Socket.IO server (usually the same host as the API, without the /api suffix)
VITE_SOCKET_URL=http://localhost:5000
